# 在这个仓库里干活的约定

给任何 agent（Claude Code / Codex / 别的 CLI）看的；人读也适用 ——
这些不是「给 AI 的特殊规则」，是这个项目本来就这么做。

## 一、先读什么

| 顺序 | 读什么 | 为什么 |
|---|---|---|
| 1 | `.workbuddy-ai/memory/MEMORY.md` | 跨会话积累的硬事实：pi 协议约束、环境陷阱、测试纪律。**它的「当前状态」一节经常落后**，用之前先核实 |
| 2 | [`docs/`](docs/) | 各子系统的设计理由（9 篇，见 [README](README.md#文档)）|
| 3 | 任务相关的现有代码 | 现有抽象往往比任务描述里的设想更合适 |

> `.workbuddy-ai/` 是 **gitignore** 的：里面记的是**开发机特有**的东西
> （工具装在哪、代理端口、本机踩过的坑）。仓库里刻意不写这些 ——
> 可用性是运行时探测的事，不是文档里的常量。

## 二、动手之前

1. `git status` / `git log` —— 确认工作区干净、看清楚最近在做什么
2. `git fetch` / `git tag` / Release 列表 —— **远端可能已经领先**，别在旧基线上改
3. `npm test` 建基线（18 个套件，3-4 分钟）
4. 把任务相关的现有代码**读完**

**然后先输出勘察结论**：现有实现已有哪些合适的抽象、你的方案要在哪几处偏离
任务描述里的设想、为什么。

> **任务描述里的设想与现有代码冲突时，以现有代码为准。**
> 写描述的人不一定知道仓库当前长什么样；照着设想硬套，会把更合适的抽象覆盖掉。

## 三、硬约束

- **零第三方依赖。** 前端原生 ES Module（零构建、零前端依赖），后端纯 `node:http`。
  要加依赖先说服人，不要顺手加。
- **不联网。** 不查 npm registry / GitHub Release / 官网；运行时不依赖网络。
  （**唯一的例外是产品功能本身**：P5 的版本检查会读 GitHub 的公开 Release API。
  它是显式设计、可静默失败、不上传任何数据，见 [updates.md](docs/updates.md)。
  这条约束说的是**你**别去联网查资料，以及别让任何功能**依赖**网络可用。）
- **不碰 pi 本体。** pi 是**外部程序**，只通过 `pi --mode rpc` 的 stdio 通信。
  不 fork、不 monkey patch、不改它的 session schema、不动用户的全局 npm。
- **不拿用户真实数据做写操作。** 会写盘的用例一律用 `os.tmpdir()` 里的 fixture；
  真实项目只做**只读**验证。
  （这条是有血的：曾为了验证会话改名，对着用户真实项目跑了一次，往他真实的会话
  文件里追加了一行。）
- **测试不依赖真 pi。** 要探测外部程序就造 fixture —— 假的全局 npm 目录、假的 `.cmd`
  shim、注入的 `env`。断言「跑测试这台机器装了什么」的测试在 CI 上必红。
- **`npm test` 是测试入口的唯一真相。** 新套件加进 `package.json` 的 test 脚本，
  CI 只调它、不把子测试抄进 workflow（抄一份就有两个真相）。
- **改完 `public/` 或 `server/` 必须重建产物**（`npm run build:exe` /
  `npm run build:app -- --rebuild`），否则用户打开看到的还是旧的排版。
  在说「改完了」之前，要么重建，要么明确告诉用户「跑源码版才看得到」。

## 四、提交 / 推送 / 发版是三件事

- **提交**：按**回滚边界**拆（业务代码 / 测试基建 / 版本号 分开）。提交信息用中文，
  写进 `.git/COMMIT_MSG_TMP.txt` 再用 `git commit -F` —— 避免 shell 对中文与 `${}` 的转义。
- **推送**：用户没说就不推。
- **发版**：用户没说就不发。流程见 [development.md](docs/development.md)。

## 五、报告要带证据

不要只说「改完了」「全绿」：

- 受影响套件的**实际数字**（不是「全绿」三个字）
- 关键的实测输出（耗时、字节数、digest、对比结果）
- 改了 UI → **真实截图**（jsdom 不做布局、也不套用外部样式表，
  所以断言「文本包含 X」的测试全绿也说明不了排版对不对）
- 如果**断言总数变少**，说明少了哪几条 —— 这个数字变少本身就是信号
  （曾误删两个调用，smoke 从 488 掉到 458 却仍显示「全绿」）
- **跳过的断言不计入分子分母**，所以同一个套件在不同机器上会报出不同的 `N/N`，
  别据此下结论

## 六、这个项目踩过的坑（写代码时避开）

- **测试红先分清是环境还是产品。** 已经修过三处「只在特定环境才假红」的断言：
  端口被动态范围占用、进程树 `taskkill /T` 收不干净、**PID 被复用**当成同一个进程。
  共同点：**用外部状态（时间 / PID / 端口）当判据**，而这些东西在繁忙的机器上不稳定。
  判据要挑**身份**（命令行、文件内容、占位符），不要挑**序号**。
- **fixture 要照真实形状造，两个方向都出事：**
  错 fixture 会把**上游的报错**当成产品缺陷；更危险的是反过来 ——
  **把真实缺陷藏起来**。`get_tree` 的 fixture 曾经用了一个 pi 里根本不存在的形状
  （`role`/`content` 摆在条目顶层，真实形状是嵌在 `message` 下），
  于是「只读顶层」的实现全绿，而真实会话里每个消息节点都渲染成
  `message: [object Object]`，**藏了整整一版**。
  → 造 fixture 前**先去上游源码里把形状读出来**。
- **不要用「没观察到」推断「不存在」。** 没见到某个字段只说明这次没用到它，
  不代表上游不支持。能力判定用三值（是 / 否 / 未知）。
- **不要存原始 payload。** 凡是会进诊断报告、日志、错误信息的东西，只记结构
  （字段名、类型、键名），不记值 —— 报告可能被贴进公开 issue。
- **「起来了」不等于「那个程序起来了」。** 经 shell 启动的平台上，
  spawn 成功只说明**外壳**起来了。

各子系统的细节：测试分层见 [testing.md](docs/testing.md)、安全边界见
[security.md](docs/security.md)、架构与模块地图见 [architecture.md](docs/architecture.md)。
